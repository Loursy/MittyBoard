name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: mittyboard
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d mittyboard"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Run tests
        env:
          DB_URL: jdbc:postgresql://localhost:5432/mittyboard
          DB_PASSWORD: postgres
          JWT_SECRET_KEY: ${{ secrets.CI_JWT_SECRET_KEY || 'Y2ktb25seS1kdW1teS1zZWNyZXQtZm9yLXRlc3RzLW5vdC1mb3ItcHJvZA==' }}
        run: ./mvnw -B test

      - name: Build jar
        run: ./mvnw -B -DskipTests package

      - name: Build backend Docker image
        run: docker build -t mittyboard-backend:ci .

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm run build

      - name: Build frontend Docker image
        run: docker build -t mittyboard-frontend:ci .
